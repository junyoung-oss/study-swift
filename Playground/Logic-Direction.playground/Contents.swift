// A = B
// B값이 A에 들어가는것을 알 수 있음

// 예시1
let A = "이 문자가 A에 들어가서 A를 출력하니, 이 문자 자체가 나오게 되는 것 입니다."
print(A); // 출력값 = 이 문자가 A에 들어가서 A를 출력하니, 이 문자 자체가 나오게 되는 것 입니다.

// 예시2
var foodColor = "노란색"; // 변수선언
var fooditName = "바나나"; // 변수선언
foodColor = fooditName // 바나나의 값이 foodColor 값에 덮어 씌어짐
print(foodColor) // 출력값 = 바나나

// 예시3
// 변수를 여러개를 한번에 선언 하고 그 값을 할당 하는 방법
var a=1, b=2, c=3;


